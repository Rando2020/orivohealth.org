import { foundation as f, risk as r, scenario as s, technical as t, troubleshoot as d, type DeploymentQuestionSeed } from './deploymentQuestionTypes'

export const deploymentQuestionSeeds2: Record<string, DeploymentQuestionSeed[]> = {
  m5: [
    f('What does a Git commit identify?', 'A specific versioned snapshot with parent history and metadata', ['A moving branch name only', 'A production environment', 'A user session'], 'A commit provides immutable revision identity within repository history.', 'github-pull-requests'),
    f('What is a branch?', 'A movable reference used to develop or organize a line of changes', ['A deployable binary package', 'A database backup', 'A test user role'], 'Branches move as commits are added, so they are weaker release identity than a commit or tag.', 'github-pull-requests'),
    f('What is the main purpose of a pull request?', 'Propose, discuss, review, validate, and merge a change between branches', ['Store production secrets', 'Replace automated testing', 'Create a database index automatically'], 'A pull request is a collaborative change and evidence record.', 'github-pull-requests'),
    f('What is a protected branch?', 'A branch governed by rules such as required reviews, checks, and restricted pushes', ['A branch that cannot contain code', 'A browser bookmark', 'A database schema'], 'Protection turns repository policy into enforceable merge or push controls.', 'github-protected-branches'),
    f('What does a tag usually provide in release management?', 'An immutable name pointing to a specific commit', ['A moving list of open issues', 'A production password', 'A feature-flag rule'], 'Tags can identify a release revision when governed and not moved.', 'github-pull-requests'),
    f('Under Semantic Versioning, what change normally increments the major version?', 'A backward-incompatible change to the declared public API', ['A compatible bug fix only', 'A documentation typo', 'A backward-compatible feature addition'], 'SemVer major changes represent incompatible public API changes.', 'semver-2'),

    t('UAT approved commit A, but production deployed commit B. Which evidence gap is most serious?', 'The approval is not tied to the deployed revision', ['The branch name is too short', 'The repository has a README', 'The release uses HTTPS'], 'Test evidence must identify the exact revision or artifact it covers.', 'github-pull-requests'),
    t('Which repository chain provides the strongest release traceability?', 'Issue or requirement, pull request, commit SHA, workflow run, artifact digest, deployment record', ['Branch name and meeting date', 'Developer name and screenshot', 'Project status and browser version'], 'The chain connects intent to verified and deployed output.', 'github-pull-requests'),
    t('A PR has green lint checks but changes a database migration and API contract. What is missing?', 'Risk-matched migration, contract, integration, and regression checks', ['A longer PR title', 'More emojis in comments', 'A second branch name'], 'Green checks are only meaningful when they cover the change risk.', 'github-protected-branches'),
    t('What should happen after a merge conflict is resolved?', 'Review the resulting diff and rerun required checks on the new combined commit', ['Assume both prior test results remain valid automatically', 'Delete the pull request', 'Disable branch protection'], 'Conflict resolution creates a new result that needs evidence.', 'github-pull-requests'),
    t('Why can a revert fail to restore a working system?', 'It may undo code but not database, configuration, queue, or external side effects', ['Git cannot create new commits', 'Tags always delete databases', 'Branches cannot be merged'], 'Version control cannot automatically reverse all deployed state.', 'github-pull-requests'),
    t('Which release note best communicates compatibility?', 'Version 3.0 removes the legacy field after the documented deprecation period; affected clients must migrate before the date shown', ['Several changes were made', 'All tickets are complete', 'The code is newer'], 'Compatibility notes identify behavior, affected consumers, and required action.', 'semver-2'),
    t('A hotfix is merged only to production and not to the main development line. What risk remains?', 'A later release can reintroduce the defect because histories diverged', ['DNS may stop resolving', 'The database must lose data', 'The client cannot use HTTPS'], 'Emergency changes should be reconciled into ongoing development.', 'github-pull-requests'),

    s('A developer adds commits after a PR approval. What should determine whether approval remains valid?', 'Repository policy plus review of the new diff and rerun of affected checks', ['The original approval remains valid forever', 'The commit author decides alone', 'The project deadline'], 'Material changes after approval require renewed evidence according to policy.', 'github-protected-branches'),
    s('A PR changes an authorization file but no security owner reviewed it. Which control should apply?', 'Code-owner or accountable security review plus required checks before merge', ['Direct push to main', 'Disable comments', 'Hide the file from reviewers'], 'Sensitive paths need appropriate ownership and review.', 'github-protected-branches'),
    s('A release tag says v2.4.0, but the artifact was rebuilt from a later commit. What is the correct response?', 'Stop promotion until tag, commit, build, and artifact identity are reconciled', ['Deploy because the label matches', 'Rename the artifact only', 'Delete the workflow logs'], 'Version labels should map to one exact revision and artifact.', 'semver-2'),
    s('A one-line change disables tenant authorization. How should the PR be treated?', 'As high impact despite small code size, with security review and targeted tests', ['As trivial because the diff is short', 'As documentation only', 'As a database backup task'], 'Risk depends on behavior and trust boundaries, not line count.', 'nist-ssdf-1-1'),
    s('A merge conflict combines two different status-precedence rules. What should the resolver do?', 'Confirm intended business behavior with owners, resolve explicitly, and run focused and regression tests', ['Choose the shorter code', 'Accept both rules silently', 'Skip testing because each branch passed separately'], 'The combined behavior is new and must reflect the intended requirement.', 'github-pull-requests'),
    s('Production needs an urgent hotfix. Which workflow is most defensible?', 'Focused branch, accountable review, targeted checks, controlled deployment, and reconciliation into main', ['Direct manual edit with no record', 'Shared admin credentials and no test', 'Delete the incident after recovery'], 'Urgency can reduce scope and timing but should preserve reproducibility and evidence.', 'github-pull-requests'),
    s('A client integration breaks after a minor version removed a field. What should change?', 'Treat removal as incompatible or provide a deprecation and backward-compatible transition', ['Continue labeling removals minor', 'Hide the error in the UI', 'Change the client identifier'], 'SemVer minor releases should remain backward compatible for the declared public API.', 'semver-2'),
    s('A reviewer approves based only on the summary and does not inspect changed migration files. What review gap exists?', 'Evidence and affected-file review did not match the change risk', ['The PR should contain no summary', 'Migrations never require review', 'Reviewers should not see tests'], 'Review should inspect the areas that create material behavior and data risk.', 'github-pull-requests'),
    s('A branch name release-final is used as the only deployment identity. What should be added?', 'Exact commit SHA, artifact digest, workflow run, and deployment record', ['A new color for the branch', 'A user password', 'A DNS alias'], 'A moving branch name cannot prove the deployed revision.', 'github-pull-requests'),

    d('A reverted application now fails because the schema no longer contains an old column. What failed in rollback planning?', 'Code and database compatibility were evaluated separately and incompletely', ['The tag used too many digits', 'The branch contained comments', 'The UI had a button'], 'Rollback must include all changed state.', 'github-pull-requests'),
    d('Required status checks do not appear on a PR. What should be checked first?', ['Workflow triggers and branch filters', 'Ruleset or branch-protection configuration', 'Whether the check name matches the required status'], ['The database row count'], 'Missing checks often result from workflow scope or protection configuration.', 'github-protected-branches'),
    d('A merge conflict was resolved, but the final commit was never built. What is the immediate control failure?', 'The resulting release candidate lacks CI evidence', ['The repository has too many branches', 'The user needs a new password', 'The API should use XML'], 'The final combined revision must receive evidence.', 'github-pull-requests'),
    d('A hotfix appears in production but cannot be found in source history. What is the operational risk?', 'The state is not reproducible, reviewable, or protected from later overwrite', ['The domain becomes invalid', 'The database automatically encrypts', 'The release notes become longer'], 'Production should be reproducible from governed source and deployment records.', 'nist-ssdf-1-1'),
    d('A patch release changes the public response schema incompatibly. What is the classification error?', 'The version communicates compatibility that the change does not preserve', ['The commit message is too short', 'The database has an index', 'The build uses a runner'], 'SemVer patch changes should preserve public API compatibility.', 'semver-2'),

    r('Which repository practice creates the greatest release-integrity risk?', 'Deploying an unreviewed local build that is not tied to a repository commit', ['Using protected branches', 'Recording artifact digests', 'Requiring pull-request reviews'], 'Untraceable local output breaks the source-to-deployment trust chain.', 'nist-ssdf-1-1'),
    r('Which pull-request claim is misleading?', 'All checks are green, so no human review or compatibility assessment is needed', ['Checks provide configured automated evidence', 'Review can assess business and architectural risk', 'Required checks should match change scope'], 'Automation does not replace all judgment or prove coverage beyond configured checks.', 'github-protected-branches'),
    r('Which hotfix behavior should be rejected?', 'Fix production directly and leave the repository unchanged because the incident is over', ['Use a focused emergency path', 'Add follow-up tests', 'Reconcile the fix into normal history'], 'Unreconciled production changes create hidden drift and recurrence risk.', 'github-pull-requests'),
  ],

  m6: [
    f('What is a software build?', 'A controlled transformation of source and dependencies into runnable or testable output', ['A production incident meeting', 'A user authorization rule', 'A DNS lookup'], 'Builds transform declared inputs into output.', 'nist-ssdf-1-1'),
    f('What is a build dependency?', 'A library, tool, runtime, or external input required to create or test the software', ['A client meeting invite', 'A release-note heading', 'A dashboard color'], 'Dependencies influence build behavior and reproducibility.', 'nist-ssdf-1-1'),
    f('What is a deployable artifact?', 'An immutable package or output promoted to an environment', ['A moving branch name', 'A user story title', 'A browser session'], 'Artifacts are the actual release outputs, not planning labels.', 'owasp-cicd-security'),
    f('What does an artifact digest provide?', 'A content-derived identity used to verify exact artifact integrity', ['A list of feature owners', 'A production password', 'A test environment name'], 'A digest helps prove the package was not changed or confused with another.', 'owasp-cicd-security'),
    f('What is artifact promotion?', 'Moving the same tested artifact through controlled environments and approvals', ['Rebuilding different code for each environment', 'Copying production data into development', 'Changing a branch name'], 'Promotion reduces untested differences by preserving artifact identity.', 'github-deployments'),
    f('What is artifact retention?', 'The policy for preserving packages and evidence for deployment, rollback, audit, and investigation', ['A UI sorting rule', 'A database join type', 'A feature-flag target'], 'Retention supports recoverability and traceability.', 'nist-ssdf-1-1'),

    t('Which input most directly improves build reproducibility?', 'A committed dependency lock or approved version policy used in a clean runner', ['A developer memory of installed packages', 'A mutable latest dependency reference', 'A screenshot of a successful build'], 'Controlled dependency versions and clean execution reduce hidden variation.', 'nist-ssdf-1-1'),
    t('Why is rebuilding separately for production risky?', 'The production artifact may differ from the artifact that passed staging and UAT', ['Production cannot run packages', 'Tags do not work in production', 'Databases reject all rebuilt code'], 'Promotion should preserve the tested candidate.', 'github-deployments'),
    t('Which artifact metadata is most valuable?', ['Source commit', 'Build run and tool versions', 'Digest and tests', 'Promotion and deployment history'], ['The developer’s favorite editor'], 'The metadata links source, build, evidence, and deployment.', 'owasp-cicd-security'),
    t('A mutable latest image tag points to different content over time. What control is stronger?', 'Deploy by immutable digest and record the human-readable version as metadata', ['Trust the tag name alone', 'Delete old digests', 'Rebuild after approval'], 'Digest identity prevents a moving label from changing the actual candidate.', 'owasp-cicd-security'),
    t('Which pipeline evidence distinguishes a compile failure from a packaging failure?', 'The stage, command, exit code, and first causal error', ['The project deadline', 'The number of users', 'The database table count'], 'Stage and command context locate the failure domain.', 'github-actions-reference'),
    t('Which permission model best protects artifacts?', 'Restricted write access, separate deploy authority, and verified read by promotion jobs', ['Everyone can overwrite packages', 'Artifacts are emailed manually', 'The public can delete old versions'], 'Artifact stores are part of the release trust boundary.', 'owasp-cicd-security'),
    t('What should happen to the last known-good artifact before a high-risk release?', 'Retain and verify it remains compatible with current configuration and data', ['Delete it to save space', 'Assume it is compatible forever', 'Rename it latest'], 'Rollback candidates need both availability and compatibility evidence.', 'github-deployments'),

    s('Staging and production use the same commit but different dependency versions. What should the team do?', 'Build one controlled candidate with declared dependencies and promote it through both environments', ['Accept the difference because the commit matches', 'Delete the lock file', 'Build manually on production'], 'A commit does not control undeclared dependency variation.', 'nist-ssdf-1-1'),
    s('A registry contains three packages labeled 4.2. Which release evidence is required?', 'Use digest, source commit, build run, tests, and deployment history to identify the approved package', ['Choose the newest filename', 'Ask which icon looks correct', 'Deploy all three'], 'Mutable or duplicate labels are not sufficient identity.', 'owasp-cicd-security'),
    s('A production bundle hard-codes a production endpoint, so it is rebuilt after UAT. What is the better design?', 'Externalize the endpoint as controlled configuration and promote the tested artifact', ['Continue special builds', 'Store the endpoint in a screenshot', 'Remove staging'], 'Deploy-specific values should not require new untested code output.', 'twelve-factor-config'),
    s('A build downloads a script from an unpinned URL. What should the pipeline owner do?', 'Use an approved pinned source or verified checksum and review the supply-chain risk', ['Trust the URL forever', 'Disable build logs', 'Copy the script into a ticket'], 'Mutable third-party build inputs can alter the artifact without source changes.', 'owasp-cicd-security'),
    s('A package is overwritten in the artifact registry after approval. What should happen?', 'Treat the candidate as invalid, investigate access, rebuild from reviewed source, and strengthen immutability', ['Deploy it because the version matches', 'Delete the audit log', 'Change the release date only'], 'Overwriting breaks artifact integrity and approval traceability.', 'owasp-cicd-security'),
    s('A packaging step says dist directory not found after compile success. What is the first useful hypothesis?', 'The build output path, working directory, or artifact configuration does not match the package step', ['The client role is missing', 'DNS expired', 'The database needs a new index'], 'The evidence points to package-stage file location.', 'github-actions-reference'),
    s('A release artifact is retained for only one day, but the monitoring window is seven days. What is the control gap?', 'Retention does not protect rollback and incident investigation through the observation period', ['The UI has too many pages', 'The branch is protected', 'The API uses JSON'], 'Retention should align with recovery and evidence needs.', 'nist-ssdf-1-1'),
    s('A staging deployment passes using artifact digest X, but production references version name only. What should the deployment job enforce?', 'Resolve and verify the version maps to digest X before deployment', ['Choose any package with the name', 'Rebuild a new package', 'Ignore staging evidence'], 'The production candidate must be the exact tested artifact.', 'owasp-cicd-security'),
    s('A build log prints an environment variable containing a secret. What should occur?', 'Restrict log access, rotate the secret, remove unsafe output, and review pipeline permissions', ['Rename the variable', 'Wait for someone to misuse it', 'Publish the log in release notes'], 'Printed secrets should be treated as compromised.', 'owasp-cicd-security'),

    d('The same source commit produces a different frontend bundle on two runs. What should be compared?', ['Dependency lock and registry resolution', 'Runtime and build-tool versions', 'Environment inputs and generated timestamps'], ['Client UAT comments only'], 'Reproducibility failures come from uncontrolled inputs or nondeterministic build behavior.', 'nist-ssdf-1-1'),
    d('A deployment record lists version 5.0 but no artifact digest. What is missing?', 'Immutable identity that proves exactly what content was deployed', ['A project logo', 'A database alias', 'A browser cookie'], 'A human-readable version alone can be ambiguous or mutable.', 'owasp-cicd-security'),
    d('Tests pass, but artifact upload is empty. Which stage should own the first investigation?', 'Packaging or artifact-publication configuration', ['Business acceptance', 'DNS administration', 'Client identity'], 'The failure occurs after tests at package or upload boundaries.', 'github-actions-reference'),
    d('A rollback artifact exists but fails with current schema. What assumption was wrong?', 'Retention was treated as sufficient without compatibility validation', ['Digests should never be used', 'Branches cannot contain old code', 'UAT caused schema drift'], 'A retained package is useful only if it can operate with current state.', 'github-deployments'),
    d('A build works only on one developer laptop. What is the likely maturity gap?', 'Undeclared local state or dependencies prevent a controlled reproducible build', ['The domain is too short', 'The database has too many rows', 'The release uses semantic versioning'], 'Release builds should not depend on one machine’s hidden state.', 'nist-ssdf-1-1'),

    r('Which artifact practice creates the highest supply-chain risk?', 'Allowing a mutable package to be overwritten after tests and approval', ['Verifying a digest', 'Restricting registry write access', 'Retaining provenance'], 'Post-approval mutation can replace trusted output with unreviewed content.', 'owasp-cicd-security'),
    r('Which release claim is unsupported?', 'The artifact is safe because its filename contains approved', ['Integrity should be verified with controlled identity', 'Source and build evidence should be linked', 'Artifact access should be governed'], 'A filename is not integrity evidence.', 'owasp-cicd-security'),
    r('Which promotion design should be rejected?', 'Rebuild from source independently in each environment and assume outputs are equivalent', ['Build once and promote', 'Separate configuration from artifact', 'Record digest and deployment history'], 'Independent rebuilds create untested variation.', 'github-deployments'),
  ],

  m7: [
    f('What is continuous integration?', 'Frequent integration of changes with automated evidence that detects defects early', ['Automatic production release of every change', 'A manual monthly backup', 'A client training environment'], 'CI focuses on integrating and validating changes.', 'jenkins-pipeline'),
    f('What is a workflow trigger?', 'An event, schedule, or approved manual action that starts automation', ['A database row', 'A release-note paragraph', 'A browser tab'], 'Triggers determine when and for which context a workflow runs.', 'github-actions-reference'),
    f('What is a runner or Jenkins agent?', 'The execution environment that performs workflow or pipeline jobs', ['The source-code requirement', 'The production database schema', 'The client acceptance criterion'], 'Runners and agents supply compute and tooling for automation.', 'github-actions'),
    f('What is a CI job?', 'A collection of ordered steps executed in a defined environment', ['A feature flag', 'A database table', 'A customer role'], 'Jobs organize automated work and can depend on one another.', 'github-actions-reference'),
    f('What is a quality gate?', 'A policy that allows or blocks progress based on defined evidence and thresholds', ['A graphic on a dashboard', 'A tag name only', 'A user password'], 'Quality gates connect evidence to authority.', 'nist-ssdf-1-1'),
    f('What is static analysis?', 'Examination of code or artifacts without executing the application workflow', ['A live UAT session', 'A production rollback', 'A DNS health check'], 'Static analysis can identify patterns and defects without running the full system.', 'nist-ssdf-1-1'),

    t('Which GitHub Actions field makes deploy wait for test?', 'needs', ['runs-on only', 'name only', 'timeout-minutes only'], 'The needs relationship defines job dependency.', 'github-actions-reference', { code: "jobs:\n  test:\n    runs-on: ubuntu-latest\n  deploy:\n    needs: test\n    runs-on: ubuntu-latest" }),
    t('Which workflow permission strategy is strongest?', 'Set explicit least-privilege permissions at workflow or job scope', ['Use write-all by default', 'Store a personal token in source', 'Disable audit logs'], 'Explicit minimum permissions reduce pipeline blast radius.', 'owasp-cicd-security'),
    t('Why is a self-hosted runner a sensitive trust boundary?', 'It may access private networks, credentials, and persistent workspace state', ['It cannot run commands', 'It has no operating system', 'It automatically removes all risk'], 'Self-hosted execution requires strong isolation and governance.', 'github-actions'),
    t('Which Jenkins Declarative element groups meaningful delivery phases?', 'stage', ['agent only', 'environment only', 'options only'], 'Stages organize build, test, package, and deploy phases.', 'jenkins-pipeline-syntax', { code: "pipeline {\n  agent any\n  stages {\n    stage('Test') { steps { sh 'npm test' } }\n  }\n}" }),
    t('What does a green CI check prove?', 'The configured job passed for the recorded revision and inputs', ['The software has zero risk', 'All requirements were tested', 'Production will never fail'], 'A check’s meaning is limited to its configured scope.', 'github-protected-branches'),
    t('Where should a production secret be scoped in a workflow?', 'Only to the authorized deployment job and protected environment that requires it', ['Every pull-request test', 'A public repository variable', 'A console echo step'], 'Secret access should be minimal and delayed until protection passes.', 'github-deployments'),
    t('Which CI output is most useful for review?', 'Test reports, scan results, build logs, artifact identity, and workflow status tied to the commit', ['A project meeting invite', 'A browser theme', 'A developer vacation calendar'], 'Review needs traceable technical evidence.', 'github-actions-reference'),

    s('A PR changes code but no workflow starts. What should the team inspect?', ['Pull-request event trigger', 'Branch and path filters', 'Workflow validity and required-check name'], ['Database control totals'], 'Workflow scope and protection configuration determine whether checks run.', 'github-actions-reference'),
    s('Deploy and test run in parallel. What is the required correction?', 'Add a dependency so deploy starts only after required tests succeed', ['Rename the deploy job', 'Move tests after production', 'Grant more permissions'], 'Evidence must precede the protected action.', 'github-actions-reference'),
    s('A third-party action uses a floating main reference. What should governance require?', 'Review and pin the action according to approved version or commit policy', ['Trust every future update automatically', 'Disable source review', 'Store secrets in inputs'], 'Mutable external workflow code can change unexpectedly.', 'owasp-cicd-security'),
    s('A scanner finds a critical dependency issue believed unreachable. What is the best release decision process?', 'Assess version, reachability, exploitability, fix, exposure, compensating controls, and time-bound exception authority', ['Ignore every scanner finding', 'Block forever without review', 'Lower severity without evidence'], 'Risk disposition should be evidence-based and governed.', 'nist-ssdf-1-1'),
    s('A Jenkins stage prints all environment variables while credentials are bound. What should happen?', 'Stop exposure, rotate potentially leaked credentials, remove broad output, and reduce credential scope', ['Keep the log public', 'Rename the credential ID', 'Disable tests'], 'Credential masking does not justify unsafe logging.', 'jenkins-pipeline'),
    s('A workflow triggered from an untrusted fork can access a privileged self-hosted runner. What is the key risk?', 'Unreviewed code may execute within a trusted network and credential boundary', ['The PR title may be long', 'The database uses SQL', 'The artifact has a digest'], 'Runner trust must match source trust.', 'owasp-cicd-security'),
    s('A lint failure blocks release while an accepted critical vulnerability does not. What should be reviewed?', 'Quality-gate priorities, thresholds, and exception governance', ['The project logo', 'The release date format', 'The number of branches'], 'Gates should reflect material risk rather than arbitrary tool status.', 'nist-ssdf-1-1'),
    s('A scheduled workflow finds dependency drift not seen in PR checks. What value does it provide?', 'It detects time-based changes in external inputs even when source code is unchanged', ['It replaces all pull-request checks', 'It guarantees production health', 'It removes the need for lock files'], 'Scheduled evidence can detect new vulnerability or dependency information.', 'github-actions-reference'),
    s('A manual workflow can deploy any branch to production. What controls should be added?', 'Approved source restrictions, protected environment, required evidence, least permissions, and recorded approver', ['A shorter button label', 'More repository stars', 'A public secret'], 'Manual actions should not bypass release policy.', 'github-deployments'),

    d('A workflow passes on push but never on pull requests. What is most likely missing?', 'A pull_request trigger or correct branch scope', ['A database migration', 'A feature flag', 'A client account'], 'The event configuration determines when the workflow runs.', 'github-actions-reference'),
    d('A Jenkins pipeline fails before any stage because no suitable agent is available. Which failure domain is primary?', 'Runner or agent allocation and infrastructure', ['Product acceptance criteria', 'Database business logic', 'Release notes'], 'The pipeline could not obtain an execution environment.', 'jenkins-pipeline-syntax'),
    d('A test job passes but its report is unavailable to reviewers. What CI control is missing?', 'Artifact or test-report publication and retention', ['A new domain', 'A client password', 'A feature flag'], 'Evidence should be retained and accessible.', 'github-actions-reference'),
    d('A secret-scanning alert is suppressed permanently with no owner. What is wrong?', 'Exception governance lacks rationale, expiration, and accountability', ['The scan ran too early', 'The repository uses branches', 'The code has tests'], 'Suppression should be governed and revisited.', 'nist-ssdf-1-1'),
    d('A workflow job receives 403 from GitHub API after permissions were tightened. What should be checked?', 'The job’s explicit token permissions against the API operation it performs', ['Database indexes', 'Client UAT data', 'DNS TTL'], 'Least privilege may need a specific approved permission rather than broad restoration.', 'github-actions-reference'),

    r('Which CI configuration presents the highest credential risk?', 'Untrusted pull-request code executes on a runner with production secrets', ['Read-only tests on a hosted runner', 'Environment-scoped deployment secrets', 'Explicit token permissions'], 'Untrusted code and privileged credentials must not share a boundary.', 'owasp-cicd-security'),
    r('Which quality-gate statement should be rejected?', 'Passing one scanner proves the pipeline and application are secure', ['Layered checks provide different evidence', 'Exceptions need governance', 'Pipeline integrity matters'], 'No single scanner proves complete security.', 'owasp-cicd-security'),
    r('Which required-check design is weakest?', 'A check named full-validation that runs only a formatting command', ['Contract tests for API changes', 'Migration validation for schema changes', 'Artifact integrity verification'], 'Check labels can mislead when implementation does not match the claimed scope.', 'github-protected-branches'),
  ],

  m8: [
    f('What is continuous delivery?', 'Keeping software in a deployable state through automated evidence and promotion, with production release potentially requiring approval', ['Automatically deploying every commit to production by definition', 'A manual code backup', 'A client sandbox only'], 'Delivery and deployment differ at the production release boundary.', 'jenkins-pipeline'),
    f('What is continuous deployment?', 'Automatically releasing every qualifying change to production without a separate manual release decision', ['Running tests on pull requests only', 'Creating release notes manually', 'Using a staging environment'], 'Continuous deployment extends automation through production release.', 'jenkins-pipeline'),
    f('What is a deployment environment in GitHub Actions?', 'A named deployment target with history and optional protection, variables, and secrets', ['A source-code branch only', 'A database row', 'A browser cache'], 'Environments represent controlled deployment targets.', 'github-deployments'),
    f('What is deployment concurrency?', 'A rule limiting overlapping workflow or job execution for the same protected resource', ['The number of users in UAT', 'A version number', 'A database join'], 'Concurrency prevents conflicting simultaneous changes.', 'github-actions-reference'),
    f('What is a deployment approval?', 'An accountable decision allowing a protected environment job to proceed after evidence is available', ['A replacement for all tests', 'A user password', 'A build dependency'], 'Approval should evaluate evidence and release authority.', 'github-deployments'),
    f('What is a rollback trigger?', 'A predefined technical or business condition that initiates recovery action', ['Any minor log message', 'A branch name', 'A README update'], 'Triggers support timely decisions under pressure.', 'aws-deployment-strategies'),

    t('Which model best describes continuous delivery with manual production approval?', 'Build and test automatically, create one releasable artifact, promote through environments, then require evidence-based approval', ['Rebuild manually in production', 'Skip CI and approve source code directly', 'Deploy every branch automatically'], 'Continuous delivery automates readiness while retaining a deliberate production boundary.', 'jenkins-pipeline'),
    t('When do GitHub environment secrets become available to a protected job?', 'After the job references the environment and required protection rules pass', ['During every pull request', 'When a branch is created', 'When the repository is cloned'], 'Environment protection can delay secret access until approval.', 'github-deployments'),
    t('What does a production concurrency group protect?', 'The environment from overlapping deployments that could conflict', ['The database from every read query', 'The branch from all commits', 'The UI from caching'], 'Concurrency is especially important for migrations and traffic changes.', 'github-actions-reference'),
    t('Which evidence should a promotion record contain?', 'Artifact digest, configuration version, source revision, approver, environment, timestamps, and result', ['Only the release date', 'Only the branch name', 'Only the deployer’s name'], 'Promotion should be traceable to the exact state deployed.', 'github-deployments'),
    t('When is rollback likely weaker than roll-forward?', 'When data or schema changes make the prior application incompatible and a focused correction is safer', ['When no production change occurred', 'When documentation changed only', 'When the release has not started'], 'Recovery choice depends on current system and data state.', 'aws-deployment-strategies'),
    t('Which pipeline structure prevents production deployment before tests?', 'A deploy job depending on successful test and package jobs plus a protected environment', ['Parallel independent jobs', 'A deployment secret available globally', 'A manual note in the README'], 'Dependency and environment controls enforce evidence order.', 'github-deployments'),
    t('What does a deployment checkpoint provide?', 'A known state where the team verifies evidence before the next potentially irreversible action', ['A new versioning format', 'A database alias', 'A user role'], 'Checkpoints reduce risk during multi-step releases.', 'microsoft-safe-deployments'),

    s('Leadership asks for continuous deployment, but rollback is untested and observability is weak. What should be recommended?', 'Strengthen CI and continuous delivery first, retain approval, improve recovery and telemetry, then reassess automatic release', ['Enable automatic production immediately', 'Remove monitoring requirements', 'Deploy larger batches'], 'Production automation should match the organization’s ability to detect and recover.', 'nist-ssdf-1-1'),
    s('Two production deployments start while the first migration is running. What control is missing?', 'A production concurrency lock and migration checkpoint', ['A new tag color', 'A longer changelog', 'A client training guide'], 'Overlapping state changes can create incompatible partial state.', 'github-actions-reference'),
    s('A production job can be triggered from any branch. What should be restricted?', 'Allowed deployment branches or tags through environment protection and repository policy', ['The database query language', 'The browser viewport', 'The project title'], 'Production should accept only approved candidate sources.', 'github-deployments'),
    s('Approval occurs before test artifacts are uploaded. How should the pipeline change?', 'Make approval and deploy depend on completed evidence publication', ['Ask reviewers to guess results', 'Move tests after deployment', 'Grant more secrets'], 'Reviewers need the actual evidence before authorizing production.', 'github-deployments'),
    s('A release failed after the database migration but before the service update. What should the team verify first?', 'Migration state, current service version, compatibility, traffic, and safe roll-forward or restoration path', ['The README title', 'The client’s browser theme', 'The branch color'], 'Partial deployment state must be established before retry or rollback.', 'aws-deployment-strategies'),
    s('A previous artifact is retained but no longer works with current configuration. What should the rollback plan do?', 'Identify a compatible known-good state or use an approved roll-forward instead', ['Deploy it because it is old', 'Delete current configuration', 'Disable logs'], 'Retention alone does not prove recovery compatibility.', 'github-deployments'),
    s('A manual production approval has become a status meeting with no checklist. What is the best improvement?', 'Define evidence, thresholds, exceptions, and authority that the reviewer must evaluate', ['Remove all evidence', 'Add more attendees only', 'Approve earlier'], 'Human gates should perform clear decision work.', 'nist-ssdf-1-1'),
    s('A deployment job fails after traffic shifted. Which record is essential?', 'Checkpointed deployment log showing artifact, traffic state, health, and completed actions', ['A project budget spreadsheet', 'A source-code style guide only', 'A client contact list only'], 'Partial-state evidence supports safe containment and recovery.', 'opentelemetry-signals'),
    s('A hotfix needs production secrets. Which design is safest?', 'Use the approved protected environment and time-bound authorized job, not a developer-local token', ['Send the secret in chat', 'Commit it temporarily', 'Use a shared admin account'], 'Emergency changes still require controlled credential boundaries.', 'github-deployments'),

    d('A production job is waiting indefinitely. Required tests are green, but the environment requires a reviewer. What is the likely state?', 'Deployment protection is awaiting approval', ['The artifact necessarily failed to build', 'The database is down', 'DNS expired'], 'Protected environments can pause jobs before secrets and deployment.', 'github-deployments'),
    d('A workflow cancels an older deployment when a new one starts, leaving a migration half-applied. What is wrong?', 'Cancellation and concurrency behavior was not designed for non-atomic state changes', ['The branch name is invalid', 'The artifact has a digest', 'The release notes are too short'], 'Concurrency policy must account for safe cancellation points.', 'github-actions-reference'),
    d('A deployment retry repeats a non-idempotent traffic or configuration step. What control is missing?', 'Checkpoint-aware idempotency or explicit recovery before retry', ['A new database table', 'A semantic version', 'A UAT username'], 'Blind retry can compound partial state.', 'github-actions-reference'),
    d('The deployment record says success, but business validation failed. What status model should change?', 'Technical completion and business release verification should be separate states', ['Every deployment should be marked failed', 'Business checks should be removed', 'The artifact should be rebuilt'], 'Deployment execution and product outcome are distinct.', 'google-sre-slo'),
    d('The production secret is visible to build and test jobs. What boundary is too broad?', 'Secret scope is not limited to the protected deployment job', ['The artifact digest', 'The branch protection', 'The test report'], 'Production credentials should be available only where necessary.', 'github-deployments'),

    r('Which delivery model claim is misleading?', 'Continuous deployment is always more mature and safer than controlled delivery', ['Delivery models should match risk and recovery capability', 'Automation can improve repeatability', 'Approval can evaluate residual risk'], 'Automatic production release is not universally appropriate.', 'nist-ssdf-1-1'),
    r('Which environment design creates the highest release-authority risk?', 'Any branch can deploy and access production secrets without approval', ['Protected branches and scoped secrets', 'One deployment at a time', 'Recorded deployment history'], 'Unrestricted source and secret access removes critical controls.', 'github-deployments'),
    r('Which rollback statement should be rejected?', 'Clicking redeploy previous always restores the database, messages, flags, and external side effects', ['Recovery scope should be documented', 'Data compatibility should be tested', 'Roll-forward may be safer'], 'Platform rollback usually does not reverse every state change.', 'aws-deployment-strategies'),
  ],
}
